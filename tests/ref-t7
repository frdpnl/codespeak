> input: "call + op+ ; 1 op+ 3"
# phrase: call + op+  ; 1 op+ 3 ; 
#   0   expr: call + op+ 
#   0   seme: ( call + op+ ) 
#   0  value: ( 'call '+ 'op+ ) 
##  reduce_items (0) entry: ( 'call '+ 'op+ ) 
##  interp_seq (0) entry: ( `call `+ 'op+ ) 
##  interp_seq resolved: ( `call `+ 'op+ ) 
##  interp_seq reduced: ( `+ ) 
#   0 reduce: [Ok , `+  ]
#   0 env:
# env:
#	state: Ok 
#	op+ = `+ 
#	it = `+ 
#   1   expr: 1 op+ 3
#   1   seme: ( 1N op+ 3N ) 
#   1  value: ( 1 'op+ 3 ) 
##  reduce_items (0) entry: ( 1 'op+ 3 ) 
##  interp_seq (0) entry: ( 1 'op+ 3 ) 
##  interp_seq resolved: ( 1 `+ 3 ) 
##  interp_seq reduced: ( 4 ) 
#   1 reduce: [Ok , 4  ]
#   1 env:
# env:
#	state: Ok 
#	op+ = `+ 
#	it = 4 
> main: exit
> env:
>	state: Ok 
>	op+ = `+ 
>	it = 4 
