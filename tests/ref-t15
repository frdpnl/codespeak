> input: "define dec (b,) ; b - 1 ; end dec"
> input: "print dec"
`(dec 'b [1] ( 'b `- 1 ) ) 
> input: "define inc (a,) "
> input: "1 + a "
> input: "end inc"
> input: "1 * 2; print it"
2 
> exit
> env:
>	state: Ok 
>	it = 2 
>	dec = `(dec 'b [1] ( 'b `- 1 ) ) 
>	inc = `(inc 'a [1] ( 1 `+ 'a ) ) 
